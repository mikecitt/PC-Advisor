package rules1;

import com.pcadvisor.pcadvisorapi.dto.SurveyDTO;

rule "CPU and MB compatible"
    when
        survey: SurveyDTO($brand : cpuName.split(" ")[0])
        text: String($text : this)
    then
        survey.setTestField($brand + text);
    end